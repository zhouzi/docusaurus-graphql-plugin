{
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "prettier": "prettier . --check",
    "lint": "eslint . --ext .js,.ts,.tsx",
    "docs": "yarn --cwd ./packages/docusaurus-graphql-plugin build && yarn --cwd ./packages/scripts generateDocs && yarn --cwd ./packages/docs build"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.22.0",
    "@typescript-eslint/parser": "^4.22.0",
    "eslint": "^7.24.0",
    "prettier": "^2.2.1",
    "typescript": "^4.2.4"
  }
}
